[package]
name = "cat" # the name of the package
version = "0.1.0"    # the current version, obeying semver
authors = ["Jason Morelli-Harlan <jMorelliHarlan@gmail.com"]

[lib]
name = "cat"
path = "src/cat.rs"
test = true
doc = true

[dev-dependencies]
rand = "0.7.0"